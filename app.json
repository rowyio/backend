{
    "name": "rowy-run",
    "options": {
        "allow-unauthenticated": true,
        "memory": "2Gi"
    },
    "hooks": {
        "prebuild": {
            "commands": [
                "npm install",
                "npx tsc",
                "cd build/hooks && node prebuild"
            ]
        },
        "precreate": {
            "commands": [
                "echo 'precreate'"
            ]
        },
        "postcreate": {
            "commands": [
                "echo 'postCreateTest'",
                "cd hooks && node postcreate"
            ]
        }
    }
}