{
    "name": "rowy-run",
    "options": {
        "allow-unauthenticated": true,
        "memory": "2Gi"
    },
    "hooks": {
        "prebuild": {
            "commands": [
                "npm install",
                "npx tsc",
                "cd build/hooks && node prebuild"
            ]
        },
        "postcreate": {
            "commands": [
                "ls",
                "cd build/hooks && node postcreate"
            ]
        }
    }
}